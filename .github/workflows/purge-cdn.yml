---
name: Purge CDN

on:
  push:
    branches:
      - main
    paths:
      - 'catalog.json'
  workflow_dispatch:

permissions: {}

jobs:
  purge:
    runs-on: ubuntu-24.04
    steps:
      - run: curl -fsS "https://purge.jsdelivr.net/gh/inference-gateway/skills@main/catalog.json"
