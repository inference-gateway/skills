{
  "name": "skills-catalog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "bun scripts/build-catalog.mjs",
    "scan": "bun scripts/scan-skills.mjs",
    "serve": "bun --watch scripts/serve.mjs",
    "test": "bun test",
    "format": "prettier '**/*.md' --write",
    "format:check": "prettier '**/*.md' --check"
  },
  "dependencies": {
    "prettier": "^3.8.3",
    "gray-matter": "^4.0.3",
    "js-yaml": "^5.4.1"
  }
}
