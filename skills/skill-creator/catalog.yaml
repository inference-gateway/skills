# Catalog metadata for the locally-hosted `skill-creator` skill.
#
# `name` and `description` are pulled from SKILL.md frontmatter by
# scripts/build-catalog.mjs; only fields not in the Agent Skills spec
# frontmatter live here.
vendor: inference-gateway
license: Apache-2.0
tags:
  - meta
  - skills
  - authoring
categories:
  - developer-tools
homepage: https://github.com/inference-gateway/skills
