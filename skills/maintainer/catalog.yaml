# Catalog metadata for the locally-hosted `maintainer` skill.
#
# `name`, `description`, and `license` are pulled from SKILL.md frontmatter
# by scripts/build-catalog.mjs; only fields not in the Agent Skills spec
# frontmatter live here.
vendor: inference-gateway
tags:
  - inference-gateway
  - maintainer
  - polyrepo
  - ecosystem
categories:
  - developer-tools
homepage: https://github.com/inference-gateway/skills
